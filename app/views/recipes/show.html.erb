<form>
  <div class="container">
    <h2>Receita</h2>
    <h3><%= @recipe.name %><br></h3>
    <section "Receita">
    <div class="col-md-4">
      <div class="panel panel-primary">
        <div class="row">
          <div class="col-md-12">
            <div class="form-group">
              <label for="@recipe.kitchen.name">Cozinha: </label>
              <%= @recipe.kitchen.name %><br>
            </div>
            <div class="form-group">
              <label for="@recipe.type_of_food.name">Tipo: </label>
              <%= @recipe.type_of_food.name %><br>
            </div>
            <div class="form-group">
              <label for="@recipe.preference.name">Preferência: </label>
              <%= @recipe.preference.name %><br>
            </div>
            <div class="form-group">
              <label for="@recipe.amount.name">Serve: </label>
              <%= "#{@recipe.amount} pessoas" %><br>
            </div>
            <div class="form-group">
              <label for="@recipe.time_preparation.name">Tempo: </label>
              <%= "#{@recipe.time_preparation} minutos" %><br>
            </div>
            <div class="form-group">
              <label for="@recipe.difficulty.name">Dificuldade: </label>
              <%= @recipe.difficulty %><br>
            </div>
            <div class="form-group">
              <label for="@recipe.ingredients.name">Ingredientes: </label>
              <%= @recipe.ingredients %><br>
            </div>
            <div class="form-group">
              <label for="@recipe.preparation.name">Passo a Passo: </label>
              <%= @recipe.preparation %><br>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  <section "Foto">
    <div class="col-md-4">
      <div class="panel panel-primary">
        <div class="form-group">
          <%= image_tag @recipe.foto.url(:medium) %><br>
        </div>
      </div>
    </div>
  </section>
</div>
<p><%= link_to 'Voltar', recipes_path %></p>

</form>
